# Alien Survivor 3D

Top-down three.js test. The camera looks down. Movement stays on a plane. Ships and enemies are simple 3D meshes.

Open `index.html` through a static server (module imports need http). WASD or the stick to move. Shots are automatic.
